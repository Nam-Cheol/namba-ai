# SPEC-024 Plan

This SPEC is closed as a superseded draft.

1. Keep this package for historical context only.
2. Do not run `namba run SPEC-024` and do not prepare a PR from this draft.
3. Use `SPEC-025` as the authoritative record for the phase-1 `$namba-create` contract.
4. Track the remaining post-`SPEC-025` generator-engine work in `SPEC-026`.
