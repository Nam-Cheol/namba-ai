# Acceptance

- [ ] `namba init .` offers a guided setup flow in interactive terminals.
- [ ] `namba init . --yes ...` supports the same choices through flags.
- [ ] Generated scaffold includes `AGENTS.md`, `.agents/skills/`, `.codex/skills/`, `.codex/agents/`, `.codex/config.toml`, and `.namba/config/sections/*.yaml`.
- [ ] Generated config stores methodology, language preferences, git mode, and Codex agent mode.
- [ ] Secrets are not written into generated files.
- [ ] README explains the Codex usage flow in Korean.
- [ ] Tests covering the new init scaffold behavior are present.
- [ ] Validation commands pass.
