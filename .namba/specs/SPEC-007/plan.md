# SPEC-007 Plan

1. Research the current MoAI init wizard and Claude-specific assets.
2. Map MoAI setup choices into Codex-compatible config and generated assets.
3. Implement `namba init` wizard + non-interactive flags.
4. Generate Codex repo config, role cards, skill mirror, and mapping docs.
5. Update README and sync project artifacts.
6. Run tests and validation commands.
