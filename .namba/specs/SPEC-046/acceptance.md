# Acceptance

- [x] `namba pr "title"` creates or reuses a PR and does not post `@codex review`.
- [x] `namba pr --review "title"` creates or reuses a PR and posts exactly one Codex review request comment.
- [x] `namba pr --review "title"` is idempotent and does not create duplicate review request comments when the marker or exact command already exists.
- [x] `namba pr --review --no-sync "title"` works.
- [x] `namba pr --review --no-validate "title"` works.
- [x] `namba pr --review --remote upstream "title"` works.
- [x] Unknown `namba pr` flags still fail with the existing command usage error behavior.
- [x] Legacy `AutoCodexReview` or equivalent config cannot cause `namba pr "title"` to request review.
- [x] `namba queue start SPEC-001` does not post `@codex review`.
- [x] `namba queue start SPEC-001 --review` requests Codex review during PR handoff.
- [x] `namba queue start SPEC-001 --skip-codex-review` remains accepted as a deprecated compatibility flag and is no-op under the default no-review policy.
- [x] `namba queue start SPEC-001 --review --skip-codex-review` fails with an explicit conflict error.
- [x] Legacy `AutoCodexReview` or equivalent config cannot cause queue PR handoff to request review.
- [x] `.github/workflows/codex-review-request.yml` is removed.
- [x] No PR opened, reopened, or ready-for-review GitHub Actions workflow automatically posts `@codex review`.
- [x] The Namba review marker is non-empty and uses `<!-- namba:codex-review-request -->`, or the marker mechanism is intentionally removed with equivalent tests.
- [x] `buildReviewRequestCommentBody` emits the non-empty marker plus the normalized review command.
- [x] `isReviewRequestComment` returns true only for the normalized exact review command or comments containing the non-empty Namba marker.
- [x] An unrelated PR comment does not block a requested Codex review comment.
- [x] Tests cover PR arg parsing without `--review`, PR arg parsing with `--review`, normal PR no-review behavior, explicit PR review behavior, duplicate prevention, unrelated-comment regression, and legacy config no-op behavior.
- [x] Tests cover queue `--review` parsing, queue default no-review behavior, queue `--skip-codex-review` no-op compatibility, and queue `--review` conflict with `--skip-codex-review`.
- [x] Tests cover the absence of default GitHub Actions auto-review behavior.
- [x] Hook guard tests cover ambiguous prompt guidance, clear prompt no unnecessary guidance, dangerous bash command denial, and safe bash command allowance.
- [x] CLI help and generated docs explain `namba pr "title"` for normal PR handoff and `namba pr --review "title"` when Codex review is explicitly desired.
- [x] Queue docs explain `namba queue start SPEC-001` for normal no-review handoff, `namba queue start SPEC-001 --review` for Codex review, and `--skip-codex-review` as deprecated compatibility behavior.
- [x] Generated skills and Codex docs do not instruct Codex to request review unless the user asked for `--review` or queue `--review`.
- [x] `go test ./...` passes.
- [x] `go vet ./...` passes if still part of configured validation.
- [x] `gofmt` produces no diff.
- [x] Python hook tests run in CI or are documented as a required validation command.
- [x] Repository search for `@codex review`, `AutoCodexReview`, `skip-codex-review`, `codexReviewRequestMarker`, and `Codex review marker` shows every remaining occurrence matches the new opt-in behavior.
