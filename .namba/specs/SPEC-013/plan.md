# SPEC-013 Plan

1. Capture the current official Codex reference set and map concrete drift points against Namba's generated assets.
2. Update the rendering code and checked-in docs for upstream references, Codex usage guidance, output-contract wording, and setup guidance.
3. Rework repo-level `.codex/config.toml` generation and documentation so Namba clearly owns only the current baseline fields it intends to manage.
4. Add or update regression tests for generated docs and config output.
5. Run validation, refresh project artifacts, and sync the resulting docs.
