# Acceptance

- [ ] `namba init .` creates `AGENTS.md`, `.agents/skills/`, `.codex/skills/`, and `.namba/` Codex-ready state.
- [ ] Repo-local skill `namba` exists and documents how Namba commands are interpreted inside Codex.
- [ ] `AGENTS.md` tells Codex to treat `namba run SPEC-XXX` as Codex-native in-session execution.
- [ ] `namba doctor` reports Codex-native repository readiness.
- [ ] README explains that `namba init .` makes the project usable from Codex.
- [ ] README explains the supported status line customization path.
- [ ] Validation commands pass.