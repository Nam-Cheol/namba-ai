# SPEC-002 Plan

1. Refresh project context and keep SPEC-002 as the source of truth.
2. Replace `--full-auto` with explicit Codex approval and sandbox flags.
3. Normalize and validate system config values before runner execution.
4. Extend execution logs to include effective approval and sandbox metadata.
5. Add tests for default flags, custom flags, and invalid config values.
6. Run project sync and repository validation.