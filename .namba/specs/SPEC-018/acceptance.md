# Acceptance

- [ ] The reported issue described below is resolved:
  Codex capability preflight probes resume only when the planned run actually needs it, and resume invocations can use supported exec-level flags before `resume --last`
- [ ] Validation commands pass
- [ ] Existing behavior around the affected area is preserved
- [ ] A regression test covers conditional resume probing
- [ ] A regression test covers exec-level `profile` or `sandbox` flags on resume invocations
