# SPEC-063 Baseline

## Current State

- `README.md` and `docs/workflow-guide.md` are generated by `namba sync`.
- `README.md` contains a Local Quality Gates section that mentions `scripts/quality.sh`, eval/report artifacts, schema validation, and coverage.
- `docs/workflow-guide.md` explains the main command lifecycle and `namba sync`, but it does not link to a dedicated verification guide.
- No `docs/verification-guide.md` exists.
- `scripts/quality.sh` already runs eval/report related checks and writes default quality artifacts under a quality artifact directory.
- `.namba` contains project docs, SPEC packages, review artifacts, and generated evidence/log locations that are referenced across README and workflow docs.
- The user reported that final hook feedback can force the Namba report frame after the assistant has already prepared a detailed answer, causing important detail to disappear.

## Risk Baseline

- Generated docs can drift if implementation edits only `README.md` or `docs/workflow-guide.md`.
- Verification guidance can become misleading if it documents desired behavior rather than actual `namba eval`, `namba report`, `scripts/quality.sh`, CI, and evidence behavior.
- A thin final hook rewrite can satisfy the frame shape while losing actionable engineering detail.
- Localized README/docs generation can introduce broken links if only the English guide exists and localized navigation assumes translated siblings.
