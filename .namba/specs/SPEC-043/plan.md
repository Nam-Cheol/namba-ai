# SPEC-043 Plan

1. Inventory the documentation ownership and source-of-truth paths.
   - Confirm whether root README files are generated by `namba sync` and identify the renderer or config path if README output must change.
   - Compare current headings and content coverage across all `getting-started` and `workflow-guide` locale files.
   - Note any facts that need confirmation against CLI help, tests, or `.namba/project` docs before rewriting.

2. Define the reusable manual structure.
   - Create a locale-neutral section blueprint inspired by the Ouroboros README reference: language switcher, status link row, short intro, table of contents, quick start, command chooser, workflow phases, generated assets, collaboration defaults, and deeper references.
   - Decide which elements belong in both manual families and which belong only in getting-started or workflow-guide.
   - Keep the structure simple enough to maintain directly in Markdown.

3. Rewrite the English manual pair first.
   - Update `docs/getting-started.md` with the new first-screen hierarchy, quick-start flow, install/update/uninstall clarity, and next-reading links.
   - Update `docs/workflow-guide.md` with command-difference tables, planning and execution flow summaries, queue and review guidance, release flow, and PR/land path.
   - Preserve existing facts and command names unless executable evidence supports a correction.

4. Localize the same structure into Korean, Japanese, and Simplified Chinese.
   - Apply the same section order and link targets to `docs/getting-started.ko.md`, `docs/getting-started.ja.md`, `docs/getting-started.zh.md`, and the three localized workflow guides.
   - Keep meaning parity with natural localized wording rather than mechanical sentence-by-sentence translation.
   - Verify every locale exposes the same core decisions: what NambaAI is, which command to use, how to start, how the workflow proceeds, and where deeper references live.

5. Polish supporting reference navigation.
   - Add compact top navigation or cross-links to `docs/codex-upstream-reference.md` and `docs/moai-adk-codex-migration-analysis.md` only if it improves discovery without changing their reference purpose.
   - Link these reference docs from the manual pages where the reader would naturally need them.

6. Validate rendering, links, and parity.
   - Review Markdown for GitHub-compatible tables, anchors, language links, relative links, and code fences.
   - Run the configured repository validation: `go test ./...`, `gofmt -l "cmd" "internal" "namba_test.go"`, and `go vet ./...`.
   - Run `namba sync` if README or generated project artifacts are affected, then inspect the diff for generated ownership comments and unintended churn.
   - If no markdown link checker exists, perform a targeted manual link check with `rg` or an available local checker and document the limitation.
