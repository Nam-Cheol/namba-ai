# SPEC-004 Plan

1. Refresh project context and keep SPEC-004 as the source of truth.
2. Rewrite README as a Korean OSS onboarding document with direct install and run commands.
3. Update the module path and imports to match the public GitHub repository.
4. Add platform-specific UTF-8 console output setup for the CLI entrypoint.
5. Add a minimal test covering the UTF-8 setup call path.
6. Run project sync and repository validation.