# Change Summary

Project: namba-ai
Project type: existing
Latest SPEC: SPEC-008
Generated: 2026-03-16T17:45:00+09:00

## Release Candidate

- Recommended version: `v0.1.1`
- Base semantic version tag: `v0.1.0`
- Release branch: `main`

## Included Work

- Added explicit workflow permission handling for Codex execution.
- Expanded the init workflow with project type selection, Java language support, and interactive terminal controls.
- Added `namba fix` and `namba release` workflows with tests and release automation support.
- Added `namba update` and parallel run reporting/sync support.
- Synced README, AGENTS, skills, and project artifacts around the updated workflow.

## Release Notes

- Existing tags are not ordered by commit chronology: `v0.0.2` is newer in git history than `v0.1.0`.
- Semantic versioning logic still treats `v0.1.0` as the highest release, so the next patch candidate is `v0.1.1`.
- Validation commands were completed before release handoff.
