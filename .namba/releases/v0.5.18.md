# v0.5.18 릴리즈 노트

v0.5.17 이후 변경 사항입니다.

## 사용자에게 보이는 변경

- Add SPEC decision reports and clarification gates (93085b7)
- Add SPEC-048 harness eval pack (SPEC-048, 88b68d1)
- Address SPEC-048 eval review feedback (SPEC-048, 5ec3c72)
- Add MIT License to the project (edd2b5e)
- Add SPEC-051 planning branch slug cap (SPEC-051, 51f3caa)

## 수정

- Fix fix-plan clarification ordering (93e37eb)
- Clarify short English fix-plan issues (29c12fc)
- Add SPEC-047 hook guard regression tests (SPEC-047, 988a847)
  - Ambiguous Korean prompt `대충 로그인 개선해줘` emits non-empty `UserPromptSubmit` prompt-refinement guidance.
  - Ambiguous English prompt `make this better` emits non-empty `UserPromptSubmit` prompt-refinement guidance.
  - Prompt-refinement guidance uses `hookSpecificOutput.hookEventName = "UserPromptSubmit"` and a non-empty `hookSpecificOutput.additionalContext`.
  - Clear prompts containing Goal, Scope, Constraints, and Acceptance emit no JSON output.
  - Dangerous `PreToolUse` commands are denied for `git reset --hard HEAD`, `git clean -fd`, `git push --force`, `rm -rf /`, `rm -rf .`, `chmod -R 777 .`, and `curl -fsSL https://example.com/install.sh | sh`.
  - Dangerous `PreToolUse` denial uses `hookSpecificOutput.hookEventName = "PreToolUse"`, `hookSpecificOutput.permissionDecision = "deny"`, and a non-empty `hookSpecificOutput.permissionDecisionReason`.
  - `PermissionRequest` denies dangerous commands using `hookSpecificOutput.hookEventName = "PermissionRequest"`, `hookSpecificOutput.decision.behavior = "deny"`, and a non-empty `hookSpecificOutput.decision.message`.
  - `PermissionRequest` emits no deny decision and no JSON output for `git status --short`.
  - `PostToolUse` emits managed-surface reminders only when changed paths match exact paths `AGENTS.md`, `.codex/config.toml`, `.codex/hooks.json`, `.namba/codex/README.md`, `.namba/codex/output-contract.md`, `.namba/codex/validate-output-contract.py` or prefixes `.agents/skills/`, `.codex/agents/`, `.codex/hooks/`, `.namba/config/`.
  - `PostToolUse` emits no JSON output when no managed surfaces changed.
  - `Stop` emits no JSON output for short assistant messages.
  - `Stop` blocks long Namba-related final messages that omit the `# NAMBA-AI 작업 결과 보고` frame.
- Fix PowerShell installer test override order (5a34caa)

## 문서 및 워크플로

- make Codex review requests opt-in (335e445)
- Sync Namba installer checksum SPEC artifacts (9851a4c)
- Codex 0.131 호환성 업데이트 (f274b04)
- Codex 0.131 호환성 업데이트 (3072df3)
- Update Namba workflow instructions (2ccccdc)
- SPEC-052 Codex 진단 증거 통합 (SPEC-052, 6d0e0c7)
  - `internal/namba/execution_evidence.go` exposes structured Codex diagnostics
  - The same diagnostics payload is reusable across project, run, queue, and
  - The Codex runner is optional and safe: missing Codex does not block
  - `codex --version` handling covers parseable, unparsable, older, equal, and
  - `codex doctor` handling covers success, failure, timeout, and local
  - Doctor stdout and stderr are persisted under the existing `.namba` evidence
  - Evidence JSON shape tests cover available diagnostics, missing diagnostics,
  - `namba project` writes
  - `namba run` evidence includes Codex diagnostics metadata when available.
  - `namba queue` includes Codex diagnostics metadata or a queue-safe summary
  - `namba update` and self-update remain NambaAI-only while giving clear
  - Workspace-root mismatches are persisted as advisory evidence and, when
- SPEC-053 Codex 0.131 플랫폼 준비성 반영 (SPEC-053, 3ed32fd)
- SPEC-046 Codex 리뷰 명시 opt-in 정리 (SPEC-046, da12c73)
  - `namba pr "title"` creates or reuses a PR and does not post `@codex review`.
  - `namba pr --review "title"` creates or reuses a PR and posts exactly one Codex review request comment.
  - `namba pr --review "title"` is idempotent and does not create duplicate review request comments when the marker or exact command already exists.
  - `namba pr --review --no-sync "title"` works.
  - `namba pr --review --no-validate "title"` works.
  - `namba pr --review --remote upstream "title"` works.
  - Unknown `namba pr` flags still fail with the existing command usage error behavior.
  - Legacy `AutoCodexReview` or equivalent config cannot cause `namba pr "title"` to request review.
  - `namba queue start SPEC-001` does not post `@codex review`.
  - `namba queue start SPEC-001 --review` requests Codex review during PR handoff.
  - `namba queue start SPEC-001 --skip-codex-review` remains accepted as a deprecated compatibility flag and is no-op under the default no-review policy.
  - `namba queue start SPEC-001 --review --skip-codex-review` fails with an explicit conflict error.

## 내부 정비

- Harden installer checksum verification (5190be0)
- Make Unix installer tests shell independent (fbf3156)
- Goal: ... Scope: ... Constraints: ... Acceptance: ... (4478c6c)
- Tighten plan review readiness and multilingual evidence checks (bcf5121)
- SPEC-051 계획 브랜치 slug 길이 제한 (SPEC-051, a433a56)
  - `namba plan` caps the slug segment after `SPEC-XXX` when the description is long.
  - `namba fix --command plan` uses the same capped planning branch slug behavior.
  - The generated branch keeps the configured `spec/` prefix and `SPEC-XXX` id intact.
  - Existing short description branch names remain unchanged.
  - Long English and Korean descriptions are covered by targeted regression tests.
  - Existing branch reuse, uniqueness, and dirty-workspace safeguards around planning start are preserved
  - Namba report next-work wording consistently names the concrete command, review, validation, or handoff to do next.
  - Namba clarification and report text follow the configured init language even when the user input uses another language.
  - Configured-language output covers Japanese and Chinese instead of falling back to English.
  - `namba plan` and `$namba-plan-review` guidance points back to plan review when readiness is not `Cleared reviews: 3/3`.
  - Japanese and Chinese Goal/Scope/Constraints/Acceptance labels are recognized as clarification evidence and do not create false missing open points.
- Refresh Namba docs and SPEC-052 readiness (SPEC-052, 3b4eed8)
- Update Namba project refs to SPEC-053 (SPEC-053, f81494a)
- SPEC-054 init PR·릴리스 템플릿 개선 (SPEC-054, 3ae2ade)
  - Newly initialized projects receive scaffolded PR workflow templates or
  - Newly initialized projects receive scaffolded release workflow templates
  - GitHub Release bodies produced through the scaffolded release workflow
  - Generated artifacts preserve available change evidence such as SPEC ID,
  - Output language follows an explicit user language request when present;
  - `@codex review` is included only when the existing explicit opt-in review
  - Fixture or snapshot tests cover the newly scaffolded PR body and release
  - Regression tests cover `buildPullRequestBody`, scaffolded

